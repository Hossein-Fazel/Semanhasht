#include <iostream>
#include <string>
#include "Semanhasht.hpp"


using namespace std;

void SMHT::start()
{
    Tehran t1;
    t1.read_file() ;

    //cin >> n;

/*
    while (n--)
    {
        cin.ignore();
        getline(cin, hour);
        getline(cin, src);
        getline(cin, dst);

        try
        {
            t1.dijkstra(t1.get_value(src) , t1.get_value(dst));
        }
        catch (invalid_argument e1)
        {
            cout << e1.what();
        }
    }
    */
}